// src/app/(main)/layout.tsx
export default function MainLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="flex h-screen">
      <Sidebar /> {/* כל אזור (main) יורש את זה */}
      <main className="flex-1 overflow-auto">{children}</main>
    </div>
  )
}